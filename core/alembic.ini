[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
version_path_separator = os
file_template = %%(rev)s_%%(slug)s

# URL базы не задаётся здесь: его сообщает конфигурация Chronoscope (§36),
# чтобы миграции и приложение не могли разойтись по пути к БД.
# См. migrations/env.py.

[post_write_hooks]
