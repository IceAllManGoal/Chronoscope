[project]
name = "chronoscope"
version = "0.0.2"
description = "Chronoscope Core — local-first recorder of structured system events"
readme = "README.md"
requires-python = ">=3.13"
license = "MIT"

dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy>=2.0",
    "alembic>=1.14",
    "pydantic>=2.9",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "httpx>=0.28",
    "jsonschema>=4.23",
    "rfc3339-validator>=0.1.4",
]

[project.scripts]
# CLI (§57). Живёт в пакете Core, потому что обращается к нему же и называется
# так же; зависимости не добавляет — транспорт на стандартной библиотеке.
chronoscope = "chronoscope.cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["chronoscope"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
filterwarnings = ["error::DeprecationWarning:chronoscope.*"]
